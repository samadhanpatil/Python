3) What does the id() function return?
Is the value return by id() same for 2 variable holding the same values?
-- id() funtion returns memory address of variable. 
-- It will have same address if 2 variables holding the same value